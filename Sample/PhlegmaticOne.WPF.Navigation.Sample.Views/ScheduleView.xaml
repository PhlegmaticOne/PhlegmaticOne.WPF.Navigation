<UserControl x:Class="PhlegmaticOne.WPF.Navigation.Sample.Views.ScheduleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PhlegmaticOne.WPF.Navigation.Sample.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Orientation="Vertical" DataContext="{Binding Entity}">
            <TextBlock Text="Date" Foreground="Wheat" FontSize="24"/>
            <TextBlock Text="{Binding ScheduleDay}" Foreground="Wheat" FontSize="24"/>
            <TextBlock Text="Start hour" Foreground="Wheat" FontSize="24"/>
            <TextBlock Text="{Binding ScheduleTimeStart}" Foreground="Wheat" FontSize="24"/>
            <TextBlock Text="End hour" Foreground="Wheat" FontSize="24"/>
            <TextBlock Text="{Binding ScheduleTimeEnd}" Foreground="Wheat" FontSize="24"/>
        </StackPanel>
    </Grid>
</UserControl>
